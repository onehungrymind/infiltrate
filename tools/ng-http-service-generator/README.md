# ng-http-service-generator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ng-http-service-generator` to build the library.

## Running unit tests

Run `nx test ng-http-service-generator` to execute the unit tests via [Jest](https://jestjs.io).
