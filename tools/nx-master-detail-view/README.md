# nx-master-detail-view

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-master-detail-view` to build the library.

## Running unit tests

Run `nx test nx-master-detail-view` to execute the unit tests via [Jest](https://jestjs.io).
